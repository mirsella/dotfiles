---
name: commit
description: Commit this session's changes with a conventional commit message. Use ONLY when the user explicitly requests committing.
---

This skill mirrors the OpenCode `/commit` command, which is the canonical source of truth. OpenCode is authoritative; these instructions are not duplicated here.

Only act when the user explicitly requested committing.

Read and follow `/home/mirsella/.config/opencode/commands/commit.md`.

Additional focus: any extra instructions provided with this skill invocation.
